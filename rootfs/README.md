# Debian root filesystem

This directory will contain reproducible definitions and scripts for building
the ARM64 Debian root filesystem. Generated filesystem images are intentionally
ignored and must remain in the local work directory.
