# Development-only escape hatch for the nonpersistent recovery test image.
# The stock value remains enabled, so SELinux stays active unless the boot
# command line explicitly contains selinux=0.
CONFIG_SECURITY_SELINUX_BOOTPARAM=y
CONFIG_SECURITY_SELINUX_BOOTPARAM_VALUE=1
