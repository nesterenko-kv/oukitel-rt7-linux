# Configuration fragments

Keep minimal, reviewable kernel and userspace configuration fragments here.
Generated full configurations belong in the local work directory unless their
redistribution status and provenance are known.

`source-compat.config` contains only compatibility choices required because the
public OPPO source is not the exact OUKITEL drop. Docker-specific additions are
kept separately in `docker-required.config` and `docker-recommended.config`.
